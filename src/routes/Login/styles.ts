import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  header: { marginBottom: 30 },
  item: {
    margin: "20px auto",
  },
  itemDividerLabel: {
    padding: 10,
    fontWeight: "bolder",
  },
});
